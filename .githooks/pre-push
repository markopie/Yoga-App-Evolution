#!/bin/bash
set -euo pipefail

echo "💾 Pre-push backup..."

if [ "${RUN_DB_BACKUP_ON_PUSH:-0}" != "1" ]; then
  echo "Skipping DB backup. To run:"
  echo "RUN_DB_BACKUP_ON_PUSH=1 git push"
  exit 0
fi

python scripts/sync_and_backup.py

echo "✅ Pre-push backup complete."
exit 0